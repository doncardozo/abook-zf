<div class="row">
    <fieldset>
        <legend>List contacts</legend>

        <?php if (sizeof($contacts) == 0) : ?>
            <span>No found results.</span>
        <?php else : ?>

            <table class="table table-striped table-bordered table-condensed table-hover">

                <thead>
                    <tr>
                        <th>#</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Address</th>
                        <th>Type</th>
                        <th>Active</th>                        
                        <th style="width: 195px">action</th>                        
                    </tr>
                </thead>

                <tbody>

                    <?php foreach ($contacts as $contact) : ?>

                        <tr>
                            <td><a href="<?php echo $this->url('contact-edit', array("id" => $contact->id)) ?>"><?php echo $contact->id; ?></a></td>
                            <td><?php echo $contact->firstName; ?></td>
                            <td><?php echo $contact->lastName; ?></td>
                            <td><?php echo $contact->address; ?></td>
                            <td><?php echo $contact->contactType; ?></td>
                            <td><?php echo ($contact->active) ? "Yes" : "No"; ?></td>                        
                            <td>
                                <a class="btn btn-primary" id="edit" href="<?php echo $this->url('contact-edit', array("id" => $contact->id)) ?>">Edit</a>
                                <a class="btn btn-primary" id="delete" href="<?php echo $this->url('contact-delete', array("id" => $contact->id)) ?>">Delete</a>
                            </td>                        
                        </tr>

                    <?php endforeach; ?>                  

                </tbody>

            </table>

        <?php endif; ?>
    </fieldset>
</div>

<style type="text/css">
    a.btn.btn-primary {
        padding-bottom:0px; 
        padding-top: 0px;
        width: 90px;
    }
</style>